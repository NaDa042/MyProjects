#include <iostream>
#include <array>

using namespace std;

int main() 
{

/*

// 1 ==>> get the power of a number  : 

    int number ;
    int power ;
    int answer=1;
    cout << "enter a number :" << endl;
    cin >> number;
    cout << "enter the power number :" << endl;
    cin >> power;

    ;;
    while (power!=0)
    {
        answer*=number;
        power--;
    }
    cout << " the answer is : " << answer;
    */
   
/*
   // 2 ==>> Calculate the average of all the elements present in an array.

    int n;
    cout << "enter the number of elements on your array :" << endl;
    cin >> n;

    int nums[n] ;

    for (int i =0;i<n;i++)
    {
        cin >> nums[i];
    }

    int sum ;
    for (int y=0;y<n;y++)
    {
        sum+=nums[y];
    }
    cout << "the avarage of the array is : " << float(sum/float(n)) ;
*/

    return 0;
}
